import { createTheme } from '@material-ui/core'

const theme = createTheme({
    direction: 'ltr',
    palette: {
        primary: {
            main: '#014Fa2'
        },
        secondary: {
            main: '#E90C13'
        },
        white: '#FFFFFF'
    }
})

export default theme